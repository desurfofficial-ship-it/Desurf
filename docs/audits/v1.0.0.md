# Desurf release audit — v1.0.0

AUDIT-VERDICT: PASS
Audited-commit: 28f72872ed1cdd21a3c03be1ddbef620f82e0d92

Auditor: independent hands-on audit (Phase B of the v1.0.0 ladder), performed at the exact tree named above — the Phase A tip plus the two-line Phase B fix commit. Every claim below was re-executed at that tree, not inherited from build reports. The release-document commit that carries this file is, by necessity, a descendant of the audited commit (a document cannot contain its own hash — the fixed-point property proven during M3-A1); the gate's inclusive-ancestor check is the mechanical guarantee that this text audits the code that ships.

## Scope audited

Range `8611d6e..28f7287` — three commits, 20 files, all inside the Phase A prompt's allowed list plus the Phase B fix:

- `e105832` — version bump 0.9.0 → 1.0.0 across all pin surfaces, the `cli.ts` fallback literal, the lockfile version fields, and the version tests; S4b rewritten version-agnostically (expected values derived from `package.json` at runtime; both the example and the installed drift-watch workflows covered). The `src/` delta for the entire phase is exactly one literal.
- `5142b84` — T17 golden-master evidence vs the published 0.9.0, the cold-start recipe, and the definition-of-done evidence file.
- `28f7287` — Phase B audit fixes: the H7 run-record literal in `docs/dogfooding.md` restored to 0.9.0 (run 33619342288 installed 0.9.0; 1.0.0 has never been published), and the stale T14e test title rebased to match its post-bump scenario. Two files, two lines.

## Checklist (per docs/audits/TEMPLATE.md)

- [x] **Full suite green** — at `28f7287`: `npm run typecheck` exit 0, `npm run build` exit 0, `npm test` = 420/420 across 41 files (add-only discipline held; the only rewrites are the declared version-agnostic S4b rewrite, version-literal bumps, and the F-2 title rebase).
- [x] **Zero production dependencies** — `npm ls --prod` empty; lockfile resolved URLs 122/122 on `registry.npmjs.org` (no mirror hosts).
- [x] **Golden-master vs previous publish** — `docs/audits/t17-gm-evidence-v1.0.0.md`: published `npm pack @desurfofficial-ship-it/desurf@0.9.0` vs local 1.0.0 build; 13-row matrix covering offline PASS/REGRESSION, `--json` shapes, diff, inspect, seal, H3 exact-count budget, record (missing `--provider` 2/2, `--fill-gaps` 0/0, `--json` shapes) and accept (nothing-to-accept 1/1, missing `--yes` 2/2, `--json` shapes). Zero unintended deltas.
- [x] **Pin bump and permanent S4b fix** — every active pin surface at 1.0.0 (README, action.yml default, example workflows, both drift-watch workflows, docs pins); hidden-dir sweeps (`rg --hidden`): zero stale 0.9.0/0.8.0 literals on active pin surfaces, survivors all prescribed-historical. S4b probe: with the manifest temporarily set to 9.9.9 the test fails — the expected version is runtime-derived, so future bumps no longer require S4b edits.
- [x] **Definition-of-done, all seven boxes** — `docs/audits/v1.0.0-dod.md`: cold-start recipe and rehearsal (published-0.9.0 artifact in a pristine temporary environment; Phase C re-runs the recipe against the published 1.0.0), record/accept baseline safety, drift-watch dogfood (F-5 lifecycle plus H7, issue #11 left open as honest model-variability evidence), four-adapter multi-turn golden-master (OpenRouter/OpenAI/Anthropic/Gemini), single-page assertion set, zero runtime dependencies, exit-code contract unchanged since v0.4.
- [x] **Exit-code spot checks** — pass → 0, REGRESSION → 1 (`forbidden: billing` row), config/infra → 2 (record/accept config paths); contract unchanged.
- [x] **Release gate live behavior** — before the document existed, the root gate fired exit 1 naming `docs/audits/v1.0.0.md` (path derived from the manifest, fail-closed). Phase B preflight with this exact text: default-tag path PASS, `AUDIT_TAG=v1.0.0` path PASS naming the audited commit, negative controls — doctored audited-commit (deadbeef) → exit 1, document removed → exit 1; tree restored clean.

## Independent verification notes

The auditor's own reproduction, separate from the build agent's harness: full suite re-run at both Phase A tips (`5142b84` and `28f7287`); version sweeps with hidden directories included (35 hits for 0.9.0, 25 for 0.8.0 — none on active pin surfaces); remote release-safety checks at audit time (no v1.0.0 tag, releases API latest = 0.9.0, no publish or drift-watch runs after M5, `npm view @desurfofficial-ship-it/desurf@1.0.0` → E404); the DoD file's pointers resolved to existing tests, docs, runs, and issues; the cited green-gate run 33613345725 verified via API.

## Phase B findings and dispositions

- **F-1 (fixed in `28f7287`):** the H7 run-record bullet had been rewritten to `@desurfofficial-ship-it/desurf@1.0.0` — false history for run 33619342288 (verified via API during M5 to run on `a2224b1`, whose workflow pins `VER="0.9.0"`). Restored to 0.9.0. The :136/:149 pin-row bumps remain: current-pin semantics per the M4 precedent, required by the S4b presence contract.
- **F-2 (fixed in `28f7287`):** stale T14e title now matches its rebased scenario (AUDIT_TAG v0.9.0 vs package 1.0.0 → exit 1).
- **Accepted observations:** the S4b stale-literal sweep is gated on the 1.0.0 manifest (permitted fallback; the load-bearing VER/default assertions are fully generic); the README/cli-contract/publishing S4b checks are presence-shaped (surfaces verified correct by direct read). CI run 33619446488 on `8611d6e` failed at Build in a transient runner flake (content-superset commit `5142b84` is CI-green on the same step; local builds green at both commits); a rerun was accepted — cosmetic.

## Verdict rationale

All seven checklist items verified hands-on at the audited commit, with the Phase B findings explicitly fixed on the record rather than silently folded. The RFC-002 definition-of-done is fully ticked with repo-verifiable evidence; the exit-code contract, the zero-dependency posture, and the add-only test discipline are intact; the H1–H7 hardening is preserved (frozen workflow validation still green). The remaining release steps — committing this document, tagging v1.0.0, and the gated publish — are mechanical consequences of the gate contract, which fails closed on every path: missing document, non-PASS or contradictory verdict, unresolvable or non-ancestor commit, tag/version mismatch. No waiver surface exists.
