# Desurf release audit — v1.0.1

AUDIT-VERDICT: PASS
Audited-commit: 758f1c2207e48a20397d61d060b25199ca32596d

## Summary

Patch release: trust hardening from Phases 1–4 of the post-v1.0.0 program.

### Changes

1. **JSON Schema strict unknown-key rejection** (`src/offline.ts`)
   - Supported keywords only: `type`, `required`, `properties`, `const`, `enum`, `items`
   - Unknown/misspelled/unsupported keywords fail at load time (exit 2)

2. **EPIPE exit-code preservation** (`src/cli.ts`)
   - EPIPE no longer forces `process.exit(0)`; semantic exitCode remains authoritative

3. **Permanent regression tests**
   - `test/schema-unknown-keys.test.ts`
   - `test/epipe-exit.test.ts`

4. **Docs** — `docs/test-case-schema.md` documents supported subset

### Checklist

- [x] Full suite green (typecheck, build, three consecutive npm test runs)
- [x] Zero production dependencies
- [x] Example workflow / Action pin bumped to 1.0.1
- [x] Exit-code contract intact (0/1/2); EPIPE cannot downgrade
- [x] Sealed v2 provenance fail-closed (Phase 4)
- [x] Pack / clean-install certified

### Non-goals (intentional, unchanged)

Soft recorded drift may exit 0 with warnings; case-level multi-turn assertions evaluate last turn; v1 sidecars lack output fingerprint; empty `{}` schema is explicit any-JSON; sole `.*` regex allowed; full JSON Schema not implemented.

### Verdict rationale

P0 trust defects fixed and regression-covered. No unresolved P0/P1. Offline gate, exit-code contract, zero-runtime-deps intact.
