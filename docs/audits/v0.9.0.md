# Desurf release audit — v0.9.0

AUDIT-VERDICT: PASS
Audited-commit: 36fd7208d3b7bf61ac439432b528b079aeb48567

Auditor: independent hands-on audit (Phase B of the RFC-002 M4 ladder), performed at the exact tree named above — the M4 Phase A tip including the A8 supplement commit. Every claim below was re-executed at that tree, not inherited from build reports. The release-document commit that carries this file is, by necessity, a descendant of the audited commit (a document cannot contain its own hash — the fixed-point property proven during M3-A1); the gate's inclusive-ancestor check is the mechanical guarantee that this text audits the code that ships.

## Scope audited

Range `dca900a..36fd720` — three Phase A commits plus the supplement, 18 files, all inside the M4 prompt's allowed list:

- `dca900a` — audit-gate checkout full history (`fetch-depth: 0` in both jobs) + release guard tests S4a/S4b/S4c (remediation for M3-A1: under a shallow clone the gate's `rev-parse --verify` false-fails for any audited sha other than HEAD).
- `7abf202` — version bump 0.8.0 → 0.9.0 across pins, the `cli.ts` fallback literal, docs, examples, tests, and the previously stale `package-lock.json` version fields (stale at 0.7.0 since the v0.8.0 cycle; tarball-unaffected, now healed).
- `8e3ba88` — T16 golden-master evidence vs the published 0.8.0.
- `36fd720` — A8 supplement: record and accept surfaces added to the T16 matrix (the single gap found in the first Phase B pass).

## Checklist (per docs/audits/TEMPLATE.md)

- [x] **Full suite green** — at `36fd720`: `npm run typecheck` exit 0, `npm run build` exit 0, `npm test` = 420/420 across 41 files (test ladder 387 → 396 → 406 → 417 → 420, add-only discipline held; the only rewrites are the declared T13a version-agnostic rewrite and the version-literal bumps).
- [x] **Zero production dependencies** — `npm ls --prod` empty; lockfile resolved URLs 122/122 on `registry.npmjs.org` (no `http://` mirror hosts).
- [x] **Golden-master vs previous publish** — `docs/audits/t16-gm-evidence-v0.9.0.md`: published `npm pack @desurfofficial-ship-it/desurf@0.8.0` vs local 0.9.0 build; offline PASS/REGRESSION, `--json` shapes, diff, inspect, seal, H3 budget wording (intentional, covered by M1 T10–T12), and — after the supplement — record (missing `--provider` 2/2, `--fill-gaps` 0/0, `--json` shapes) and accept (nothing-to-accept 1/1, missing `--yes` 2/2). No unintended deltas.
- [x] **Example workflow pin bump** — examples pin 0.9.0 (S4b sweep: zero surviving 0.8.0 literals on pin surfaces).
- [x] **Exit-code spot checks** — pass → 0 (offline suite), REGRESSION → 1 (`forbidden: billing` row), config/infra ERROR → 2 (record/accept config paths above); contract unchanged.
- [x] **Release gate live behavior** — `workflow_dispatch` on the published 0.8.0 (run 33592221720): the gate job failed and the publish job was skipped via `needs:`, proving the release cannot proceed unaudited. Observed failure branch was exit 127 (the v0.8.0 tag tree predates `ci/`, so the script is absent there) rather than the predicted missing-document branch; this is mechanically correct and does not weaken the blocking property. Note for future dogfooding: dispatching tags older than the gate always exits 127 — expected.

## Independent verification notes

The auditor's own reproduction, separate from the build agent's harness: gate fired at the repo root with the exact repair block (exit 1, naming `docs/audits/v0.9.0.md`, both machine lines, TEMPLATE pointer); record/accept scenarios re-run against both binaries with byte-identical outputs; a hand-crafted pending history entry confirmed the missing-`--yes` refusal (exit 2, baseline untouched) and that `--yes` promotes it (exit 0, `acceptedAt` set). S4c permanently locks the `cli.ts` fallback version literal to `package.json`.

## Accepted variances and carried items

- **N3 (accepted deviation):** `docs/dogfooding.md:174` intentionally still says "0.8.0 pin" — it describes the currently installed drift-watch workflow, which M5 bumps; bumping it now would make the doc false. Declared here so the S4b carve-out is on the record. M5 must bump the line together with the workflow itself.
- **Carried to M5 (docs hygiene, non-blocking):** `docs/audits/TEMPLATE.md` still contains HTML entities in its filename line (`v&lt;version&gt;`), which misrender in GitHub code spans. The gate does not read this file, so the release is unaffected; fix the two lines in M5.

## Verdict rationale

All six checklist items verified hands-on at the audited commit, with the two deviations above explicitly accepted rather than silently folded. The exit-code contract, the zero-dependency posture, and the add-only test discipline are intact; the H1–H6 hardening from M1–M3 is preserved (frozen workflow validation still green). The remaining release steps — committing this document, tagging v0.9.0, and the gated publish — are mechanical consequences of the gate contract, which fails closed on every path: missing document, non-PASS or contradictory verdict, unresolvable or non-ancestor commit, tag/version mismatch. No waiver surface exists.
